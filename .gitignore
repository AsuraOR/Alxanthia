node_modules/

# Regenerated by `npm run build` — its `commit` field is meant to change on
# every build, so tracking it would make CI's "is dist/ up to date" check
# fail on every single commit for no real reason.
dist/build-info.json
